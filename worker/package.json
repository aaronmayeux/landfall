{
  "name": "landfall-warm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Landfall's warm Worker — fetches each feed once globally into KV so the Pages Functions never hit NOAA per-datacenter. SPEC §17 Pass B.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
